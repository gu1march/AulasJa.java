
public interface AreaCalculavel {
	
public default void calcularArea() {
//Interface só recebe métodos e não atributos, além de ser implementada usando (implements), é possível implementar mais de uma interface diferente da super classe//
}
	
	
	
}
